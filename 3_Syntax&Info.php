<?php

/*
 * Syntax:
 * PHPmyAdmin SQL
 * Command Line
 *
 * mysql -u root <- To Log In To MySQL
 * -p <- For Password If Exist
 * show databases; <- To See All Databases
 * CREATE DATABASE Name; <- To Create New Database
 * CREATE DATABASE IF NOT EXISTS Name; <- To Create Database If Not Exists
 * DROP DATABASE Name; <- To Delete Database;
 * DROP DATABASE IF EXISTS Name; <- To Delete Database If Exists
 * SELECT
 *      name, email, info
 * FROM
 *      TableName
 * WHERE
 *      id = 100
 * ORDER BY
 *      id
 * LIMIT
 *      1;
 */